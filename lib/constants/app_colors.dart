import 'package:flutter/material.dart';

class AppColors {
  static const Color background   = Color(0xFF0D0D0D);  // dark bg
  static const Color surface      = Color(0xFF1A1A2E);  // card bg
  static const Color primary      = Color(0xFF6C63FF);  // purple accent
  static const Color accent       = Color(0xFF00D9FF);  // cyan highlight
  static const Color textPrimary  = Color(0xFFFFFFFF);
  static const Color textSecondary = Color(0xFFB0B0B0);
}